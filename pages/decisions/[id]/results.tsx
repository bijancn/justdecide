import DecisionResult from "../../../components/DecisionResult";

export default function IndexPage({ user }) {
  return (
    <>
      <DecisionResult topicTitle="Where to go for Lunch?" />
    </>
  );
}
